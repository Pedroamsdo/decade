# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
build/
dist/

# Virtual envs
.venv/
venv/
env/

# Tests / coverage
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# IDE
.vscode/
.idea/
*.swp
*~

# macOS
.DS_Store
.AppleDouble

# Project data — bronze/silver/gold are reproducible artifacts, not source.
# EXCEPTION: ANBIMA drop XLS files are committed because the source portal
# requires reCAPTCHA + paid OAuth, so a fresh clone cannot fetch them.
data/*
!data/bronze/
data/bronze/*
!data/bronze/anbima_175/
!data/bronze/anbima_indices/
data/bronze/anbima_175/*
data/bronze/anbima_indices/*
!data/bronze/anbima_175/dropped/
!data/bronze/anbima_indices/dropped/
reports/

# Notebooks state
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/

# Secrets
.env
.env.*
!.env.example

# Lock files we don't track (we track requirements.txt instead)
uv.lock
poetry.lock

# Case study PDF — not part of the source code
*.pdf
